gpu.square(x1, y1, x2, y2, hex)
   renders a square with a color 'hex' from (x1, y1) to (x2, y2)
gpu..line(x1, y1, diameter, hex)
   renders a line with a color 'hex' and diameter 'diameter' from (x1, y1) to (x2, y2)
gpu.circle(x, y, diameter, hex)
   renders a circle with a color 'hex and diameter 'diameter' at (x, y)
cpu.vars.set(variable, value, input index)
   sets 'variable' to 'value', or 'input index'
cpu.vars.change(variable, value, input index)
   changes 'variable' by 'value', or 'input index'
cpu.loop(name, length)
   creates a loop named 'name' that runs 'length' times
cpu.loop.end(name)
   resets or ends loop 'name'
